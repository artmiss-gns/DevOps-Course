apiVersion: v1
kind: Service
metadata:
  name: {{ include "myproject01.fullname" . }}
  labels:
    {{- include "myproject01.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myproject01.selectorLabels" . | nindent 4 }}
