apiVersion: v1
kind: Pod
metadata:
  name: my-pod1
spec:
  containers:
    - name: container1
      image: busybox
      command: ['sh', '-c', 'echo $POD_IP && sleep 1000']
      env:
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
